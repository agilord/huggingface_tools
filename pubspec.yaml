name: huggingface_tools
description: Tools to search and download models from HuggingFace.
version: 0.1.0
homepage: https://github.com/agilord/huggingface_tools
topics:
  - cli
  - llm
  - tool
  - setup

environment:
  sdk: '^3.8.0'

dependencies:
  args: ^2.6.0
  http: ^1.4.0
  json_annotation: ^4.9.0
  path: ^1.9.1

dev_dependencies:
  build_runner: ^2.4.12
  json_serializable: ^6.8.0
  lints: ^5.1.1
  test: ^1.25.14
